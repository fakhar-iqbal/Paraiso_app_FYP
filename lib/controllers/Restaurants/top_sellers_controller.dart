import 'package:flutter/foundation.dart';

class TopSellersController with ChangeNotifier {
  // final RestaurantPostRepo _repo = RestaurantPostRepo();
  // List<Map<String, dynamic>> _topSellers = [];

  // List<Map<String, dynamic>> get topSellers => _topSellers;

  // Future<void> fetchTopSellers() async {
  //   try {
  //     List<Map<String, dynamic>> sellers =
  //         await _repo.findTopSalesRestaurants(5);
  //     _topSellers = sellers;
  //     notifyListeners();
  //   } catch (e) {
  //     if (kDebugMode) print('Error uploading image: $e');
  //   }
  // }
}
